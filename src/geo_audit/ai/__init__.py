"""AI 层。**本包是全仓唯一允许 import LLM client 的地方。**

## 为什么要有这条边界

前三轮实测砍掉的检查，失败原因是同一个：**让 LLM 当裁判**。
[ContraDoc](https://arxiv.org/abs/2311.09182)（NAACL 2024）实测，GPT-4 在
300–2200 token 文档里判自相矛盾的真实准确率只有 **34.7%**。本项目自己那轮
过期信号实测，21 条里推翻 1 条、降级 3 条、重复计 3 条。

结论不是「别用 LLM」，是**把 LLM 从裁判席上挪走**：

    允许 LLM 站的三个位置              永远不给 LLM 的位置
    ──────────────────────────         ──────────────────────
    出题的人   生成「用户会怎么问」      裁判  是否算缺陷
    演用户的人 扮演用户去走流程          裁判  两个值是否冲突
    提建议的人 生成「这段该怎么改」      裁判  链接是否算死
    抽取的人   文字 → 结构化五元组      裁判  是否算过期

所以判定永远写在 ``checks/`` 里，而 ``checks/`` 里没有 LLM。这不是靠自觉，
靠 ``tests/test_ai_boundary.py`` 的两条门禁守着（A52 / A54）。

## 这个包里什么能有 LLM、什么不能

    ai/questions.py     零 LLM  —— 从 v1 已抓到的站点事实反向生成问题
    ai/facts.py         零 LLM  —— 从实录快照抽站点权威值
    ai/probe.py         有 LLM  —— 唯一真发模型调用的模块
    checks/ai_perception.py   零 LLM —— 对账（模型说的 vs 站点权威值）
    checks/attribution.py     零 LLM —— 归因（BM25，可解释）

也就是说：**这一层里真正值钱的部分（对账与归因）本身不需要 LLM，
所以它离线可测、确定性可断言。** LLM 只负责去问和去演。

## 降级不许静默

没有 API key / 超预算 / 超时的时候，工具退回 v1 行为，并在报告里印明
「AI 层未运行」——**不是悄悄跳过**（A53）。「没能测」与「测了没问题」在这个
项目里从头到尾是两件事，AI 层不例外。
"""

from __future__ import annotations

__all__: list[str] = []
